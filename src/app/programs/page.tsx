import Link from "next/link";
import { PublicHeader } from "@/components/public-header";
import { WhatsAppFab } from "@/components/whatsapp-fab";
import { COURSES, WHATSAPP_DISPLAY, whatsAppLink } from "@/lib/site";

export default function ProgramsPage() {
  return <main className="public-site"><PublicHeader /><section className="public-section container"><span className="eyebrow">Programs</span><h1 className="editorial-title">Classes 9–12,<br /><em>all subjects.</em></h1><p className="editorial-lede">Complete school coaching plus NEET and JEE preparation — one focused rhythm: understand, practice, review, improve. Message us on WhatsApp at {WHATSAPP_DISPLAY} for batch timings and admission.</p><div className="path-grid">{COURSES.map((course) => <article className="path-card" key={course.tag}><span>{course.tag}</span><strong>{course.title}</strong><p>{course.body}</p></article>)}</div></section><section className="split-story"><div className="container split-story-grid"><div><span className="eyebrow light">Inside the workspace</span><h2>Notes.<br />Tests.<br /><em>Progress.</em></h2></div><div className="split-story-copy"><p>Members can move from a learning note into practice, submit assessments through a server-authoritative flow and return to progress insights without leaving the system.</p><Link href="/login" className="light-link">Enter the workspace →</Link></div></div></section><section className="public-section container"><div className="public-cta" style={{ marginBottom: 0 }}><div><span className="eyebrow">Have a question?</span><h2>Talk to us<br /><em>directly.</em></h2></div><a href={whatsAppLink("Hi Saarthi Classes, I would like to know more about admission.")} target="_blank" rel="noreferrer" className="public-button public-button-primary">WhatsApp us →</a></div></section><WhatsAppFab /></main>;
}
